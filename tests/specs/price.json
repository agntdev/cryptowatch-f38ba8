[
  {
    "name": "/price on empty watchlist shows empty state",
    "steps": [
      {
        "send": { "text": "/price" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Your watchlist is empty — tap ➕ Add coin to add one."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "Prices menu button shows empty state",
    "steps": [
      {
        "send": { "callback": "price:show" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "Your watchlist is empty — tap ➕ Add coin to add one."
            }
          }
        ]
      }
    ]
  }
]
